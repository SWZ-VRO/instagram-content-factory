# Celery tasks (folder watcher, variant generator, publishing worker) are
# introduced in Phase 2 / Phase 6 once Celery is wired into docker-compose.
# Left empty on purpose for Phase 1 -- see plan file, "Explicitly not in
# this pass".
